# Financial Sentiment Project

This folder contains the existing financial sentiment analysis project, including:

- the original `src/` training and evaluation pipeline
- the `app/` PySide6 interface
- `outputs/` with saved results
- `plans/` with project-plan drafts

The new project workspace for numerical claim detection is:

- `../numerical_claim_detection_project/`
